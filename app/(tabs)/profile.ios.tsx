
// iOS-specific profile screen - uses the same implementation as the base profile screen
// This ensures consistency across platforms while allowing for iOS-specific optimizations if needed
import ProfileScreen from './profile';

export default ProfileScreen;
