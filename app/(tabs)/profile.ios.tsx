// iOS-specific profile screen - uses the same implementation as the base profile
// This file exists to allow platform-specific customizations if needed in the future
import ProfileScreen from './profile';

export default ProfileScreen;
